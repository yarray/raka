require 'raka'

dsl = Raka.new(self, output_types: [:txt])

dsl.scope :class1, :class2 do
    dsl.scope :topic1 do
        txt.topic1 = run do |task|
            puts task.scope
        end
    end

    dsl.scope :topic2 do
        txt.topic1 = [''] | run do |task|
            puts task.scope
        end
    end
end